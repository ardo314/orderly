package ardo:orderly;

interface core {
    resource game {
        constructor();

        pieces: func() -> list<u32>;
        is-over: func() -> bool;
        tries: func() -> u32;
        correct-pieces-count: func() -> u32;

        play: func(a: u32, b: u32);
    }
}

world orderly {
    export core;
}
